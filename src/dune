(library
    (name BitarrayExt)
    (modules Bitarray BitarrayExt Chunk)
    (public_name BitarrayExt)
    (libraries base)
    (synopsis 
        "Fixed size bitarrays with common operations like add and shift."))

(executable
    (name profile)
    (libraries BitarrayExt)
    (modules Profile))

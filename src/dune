(library
    (name BitarrayExt)
    (modules Bitarray BitarrayExt)
    (public_name BitarrayExt)
    (libraries core stdlib)
    (synopsis 
        "Fixed size bitarrays with common operations like add and shift."))

(executable
    (name profile)
    (libraries BitarrayExt)
    (modules Profile))
